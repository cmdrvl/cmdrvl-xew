# Local developer configuration for cmdrvl-xew (not committed).
#
# Copy this file to `.env.local` and fill in values.
#
# Arelle taxonomy bundle cache (S3 or file):
# - If set, `cmdrvl-xew arelle install-packages` can bootstrap taxonomies from this bundle.
# - Format supports: s3://..., https://..., file:///abs/path/to/bundle.tgz, or a local path.
#
# XEW_ARELLE_BUNDLE_URI=s3://YOUR_BUCKET/xew/arelle-taxonomy-packages/xew-arelle-bundle.tgz
# XEW_ARELLE_BUNDLE_SHA256=...  # optional, recommended
#
# Optional: AWS profile used by AWS CLI when downloading s3:// bundles
# AWS_PROFILE=default
